language: java

# blacklist
# branches:
#   except:
#     - legacy
#     - experimental

# whitelist
branches:
  only:
    - develop

before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"